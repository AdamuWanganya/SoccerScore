<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moringa.soccerscore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoccerScore">
        <activity
            android:name=".Leagues"/>
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.moringa.soccerscore.MainActivity">
        </meta-data>
            android:exported="true"
        android:label="@string/title_activity_leagues"
        android:theme="@style/Theme.Leagues.NoActionBar"/>
        <activity
            android:name=".MainActivity"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>